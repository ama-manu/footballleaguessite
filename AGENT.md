## Ignore Rules (Strict)

- The directory `oldreactcode/` is explicitly excluded from all agent operations.
- Any attempt to access `oldreactcode/` must be skipped without fallback.
- No summaries, metadata extraction, or indirect usage of `oldreactcode/` contents is allowed.
